"""Zipping the finished bundle folder into a single .zip file."""

from pathlib import Path


def zipBundle(bundleDir: Path, zipPath: Path) -> Path:
    """Zip bundleDir into a single .zip file at zipPath."""
    # TODO: implement
    pass
